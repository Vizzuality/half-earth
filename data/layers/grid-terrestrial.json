{
    "is_tile_layer": false,
    "slug":"mol-terrestrial-grid",
    "category": null,
    "dataset": "grids",
    "layer_name":"Terrestrial grid",
    "description": null,
    "sql": "WITH b as (SELECT st_setsrid(st_geomfromgeojson('{{bounds}}'), 4326) as the_geom) SELECT ST_AsGeoJSON(ST_SimplifyPreserveTopology(tergrid.the_geom, 0.1)) as the_geom, tergrid.cell_id FROM terrestrial_grid as tergrid, b WHERE ST_Intersects(tergrid.the_geom, b.the_geom)",
    "cartocss": "#layer { polygon-opacity: 0; ::outline { line-color: #FFFFFF; line-width: 1; line-opacity: 0.75; } }",
    "maxZoom": 13,
    "minZoom": 3,
    "interactionConfig": {"output": [ {
                                         "format": "null",
                                         "type": "number",
                                         "property": "ID",
                                         "column": "cell_id"
                                         }
                                       ],
                                 "type": "intersection"
                             },
    "params_config": [
                        {
                            "key": "bounds",
                            "required": true
                        }
                    ]
 }

