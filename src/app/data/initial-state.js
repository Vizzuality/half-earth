import { initialState as zoom } from 'components/zoom'
import { initialState as earthSaved } from 'components/earthometer'
import { initialState as map } from 'pages/map'
import { initialState as global } from 'pages/global'

export default {
  zoom,
  earthSaved,
  map,
  global
}
