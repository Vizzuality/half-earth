import { createAction } from 'redux-tools'

export const toggleFold = createAction('toggleFold')
export const toggleValue = createAction('toggleValue')
export const updateOpacity = createAction('updateOpacity')
export const switchScreenMode = createAction('switchScreenMode')
