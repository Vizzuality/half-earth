import { createAction } from 'redux-actions'

export const showMammals = createAction('showMammals')
export const hideMammals = createAction('hideMammals')
export const toggleMammals = createAction('toggleMammals')
